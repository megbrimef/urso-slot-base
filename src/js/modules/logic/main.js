class ModulesLogicMain {
    // _stateManagerStarted = false;

    // _initScene(name) {
    //     if (name === Urso.config.defaultScene && !this._stateManagerStarted) {
    //         this._stateManagerStarted = true;
    //         Urso.statesManager.start();
    //     }
    // }

    // _initSceneHandler = (name) => this._initScene(name);

    // _subscribeOnce() {
//     this.addListener(Urso.events.MODULES_SCENES_NEW_SCENE_INIT, this._initSceneHandler, this);
    // }
}

module.exports = ModulesLogicMain;
